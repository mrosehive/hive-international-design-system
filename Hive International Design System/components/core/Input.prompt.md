Slate text field whose hairline border warms to gold on focus — for forms, contact, and search.

```jsx
<Input label="Work Email" type="email" placeholder="name@agency.gov" helper="Government addresses only" />
<Input label="Reference" error helper="Required" />
```

Optional `label` (uppercase, tracked) and `helper` text. `error` switches the border and helper to the danger color.
