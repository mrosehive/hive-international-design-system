Squared chip for capabilities, NAICS codes, contract vehicles, and filter keywords — quieter and more rectangular than a Badge.

```jsx
<Tag>Digital Forensics</Tag>
<Tag active onClick={() => toggle('border')}>Border Security</Tag>
```

Pass `onClick` to make it an interactive filter; `active` shows the gold selected state. Use rows of Tags inside a flex container with `gap`.
